# Copy to .env and adjust. Not read by server.js yet (the demo uses JSON
# files), but this is the shape db.js's real-database version expects -
# see the comment block at the top of db.js.

PORT=4000
STATE_CODE=MN

# DATABASE_URL=postgres://user:password@localhost:5432/police_monitoring
